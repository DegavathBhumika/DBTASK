CREATE TABLE BORROW_RECORDS
(
  TXN_ID         NUMBER,
  BID            NUMBER,
  MID            NUMBER,
  ISSUED_BY      NUMBER,
  ISSUED_DT      DATE                           DEFAULT sysdate,
  RETURN_STATUS  CHAR(1 BYTE)
);

ALTER TABLE BORROW_RECORDS ADD PRIMARY KEY (TXN_ID);

ALTER TABLE BORROW_RECORDS ADD (
  FOREIGN KEY (BID) 
  REFERENCES SCOTT.BOOKS (BID)
  ENABLE VALIDATE
,  FOREIGN KEY (MID) 
  REFERENCES SCOTT.MEMBERS (MID)
  ENABLE VALIDATE
,  FOREIGN KEY (ISSUED_BY) 
  REFERENCES SCOTT.STAFF (SID)
  ENABLE VALIDATE);